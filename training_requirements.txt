# Enhanced Multi-Domain LLM Training Requirements
# Install with: pip install -r training_requirements.txt

torch>=1.9.0
numpy>=1.21.0
tqdm>=4.62.0
matplotlib>=3.4.0

# Optional but recommended for faster training
# torchvision  # If using CUDA
# accelerate   # For distributed training